
# Reviewer: D2r-0b (annulus march) as written — 2026-09-23

**Right.** The wall above the nozzle plane has no thermal BC except a cooling one (h = 0,
T_gas = 293 K, losses on) — genuine missing physics, cheap to add through the single integration
point; per-bin h; scoring against a prediction; monotonicity legs; the pre-flight discipline.

**Fatal as written — the anchor is not what the packet thinks.** `side_face_h_max` is applied
ONLY when `spall.per_cell_removal` is on (`MMWSpalling.H:1433–1435`, `:2671–2673`: `side_hmax =
(side_face_flux && per_cell_removal) ? side_face_h_max : -1`). The packet keeps per-cell removal
OFF on every leg and describes C1 as "side_face_flux = 1 at default side_face_h_max = 80". So C1
and all five A legs run **uncapped f = 1 impinging h (≈ 600 W/m²K) on every step face in the band**
= D2k's `M_f10`: 4.07 m/h, absorbed power 2×, **0.40 m box consumed by ~400 s** (D2k RESULTS:146);
the legs are 600 s. Every (b)/(c)/(d) comparison would sit inside the over-powered configuration
D2l already killed, and the runs exit the box before the scoring window ends. Fix = a planner
choice: decouple the cap from `per_cell_removal` (identity-safe with side flux off), or — cleaner —
give side faces the grazing law everywhere (top = impingement below the plane, vertical = annulus
h), which is the orientation split this line has been circling.

**The omitted prior.** Side heating against the plane has been MEASURED on the arbiter: D2l f = 0.2
(≈ 140 W/m²K, T 1600) gap −6.4/−8.0/−14.0 %, D2q-2c cap 80 gap −10.9/−22.2/−30.3 %, both FAIL; f = 0.5
(≈ 350) passes. The lumped model's "9.4× at h = 150" is not consistent with that unless top heating
of the above-plane columns is the decisive addition — and there is evidence it might be: C0 (no
plane, tops at 700) with cap 80 gives −3.7/−4.5/−2.0 %, and C0 differs from C1 exactly in the
above-plane heating. So the experiment is worth running with an honest prior of "uncertain", and
it should run FIRST on the arbiter pair (3.4 + 21.7 min) where those baselines exist, not on the
Meier configuration at one mesh with the mesh pair benched.

**Direction on shape.** The band below the plane keeps impinging h, so the funnel is untouched;
added wall heat can only widen the hole above the plane and raise absorbed power in a model 1.9×
over on volume. This is a drain/mesh remedy, not a shape fix, and should be framed as one. The
predicted wall (645–770 K) is 50–180 K below firing — the D2i knife-edge again. The deferred
water-cooled shield removes ~half the annulus heat; deferring it biases every leg toward the
failure mode (over-removal). Put the shield in from the start as a second sink per bin (a 350 K
perimeter in the same march) — it is the same plumbing and it turns an "upper bound" into a result.

**Spec gaps.** No burner-body radius key exists (grep: none) — per-bin `A_ann`, `D_h` need
`r_shield = 33.5 mm` as a key; P5's widening mechanism (wall tops firing from above) is not
Meier's (floor cut beyond the skirt at floor level, D2m); the wall-T calibration S is 2 mm-specific
(D2q-2c: ~100 K hotter at 1 mm).

**Recommendation.** Do not launch wave 1 as written. (1) Fix the anchor: side faces take the
grazing/annulus h, or at minimum decouple the cap. (2) Arbiter pair with the march on, gaps vs
D2l f0.2 / D2q-2c cap-80 / C0. (3) Shield sink included. (4) Then the Meier wave in a ≥ 0.65 m
box or ≤ 400 s. (5) Wave 2 stays.

# Reviewer: D2r-0c (above-plane heating on the arbiter) — 2026-09-23

**The question is the right one and the one-variable design is right. The vehicle cannot
answer it as written.** Four facts, all from the tree:

1. **The arbiter never runs the enthalpy march.** `input_hotdisk` sets no `jet_closure` (default
   `"none"`, `:5987`) and the march is called only under `surface_patch.jet_enthalpy` (`:1774`).
   The field is prescribed (`h_expr`/`T_flame_expr`/`nozzle_z0`). An axial pass appended to
   `JetEnthalpyMarch` **would not execute on any M leg**; there is no stream (T_gas, m, mcp) to
   march. The pre-flight's stream numbers are Meier `LI0_2mm` numbers, not the arbiter's.
2. **`body_radius = 33.5 mm` exceeds the arbiter's 30 mm disk.** `A_ann` (void outside the body)
   is zero, the per-bin `h` is undefined, and the shield perimeter exceeds the hole's. The shield
   has no meaning on a flat plate with no burner. Both belong to the Meier packet.
3. **`f` multiplies `flame_h` on every side face (`:2744–2745`, `:1621`, `× side_f` at `:1633`).**
   With `f = 0.2` and above-plane `flame_h = h_ann`, above-plane **sides get 0.2·142 = 28** unless
   the side path is changed; item 5 declares the intent but the code does the opposite. And
   **D2j-0b C2/C3 already showed above-plane TOP heating is inert** (h 100, T 1000/1600: C1 ≡ C2 ≡
   C3 in every band, block and front at both meshes; tops warmed 430–500 K, the sink cells did not
   move). So the as-written experiment — tops at 142, sides at 28 — is **inert by construction**
   and would return P4 for a plumbing reason. The decisive term, if it exists, is the **side faces
   of above-plane columns at ≈ 140**: D2l f = 0.2 gave 6/8/14 % with exactly those faces unheated
   (the sink column is the taller one, above the plane, `flame_h` ≈ 0 ⇒ its exposed step faces got
   0.2 × 1e-3), while D2q-2c's no-plane C0 at cap 80 — every sink face heated — gave 3.7/4.5/2.0 %.
4. **The identity set does not exercise the side-face path** (no witness input sets
   `side_face_flux`; `unit/side_face_flux` is not in `wit.sh`), and D2q-2b/2c changed that path
   (cap gating, ledger). "Identity keeps `A_f02` valid" is therefore an inference. A **3.2-minute
   re-run of `A_f02` (2 mm) with the current binary, `cmp` against the frozen CSV**, is the proof;
   the packet forbids it for a reason that does not hold. Add `unit/side_face_flux` to the witness
   set going forward.

**Recommendation — split the packet.**
- **D2r-0c′ (arbiter, ~35 min, one small source change):** prescribed above-plane `(h, T)` through
  the existing parser (D2j-0b's C3 mechanism with h_up = 142, T = 1600) plus a **per-region side
  factor** for `s ≤ 0` columns (`side_face_factor_above`, default = `side_face_factor` so identity
  holds). Legs: `V_2mm` = `A_f02` re-run for validation; `M1` = tops 142 / sides 142 above the plane
  (f_above = 1) at 2 and 1 mm — the test; `M0` = tops 142 / sides 28 (f_above = 0.2) at 2 mm — the
  tops-vs-sides separator, predicted ≡ `A_f02` by D2j-0b. Score (a) on M1; (d) vs `A_f02`; prior
  registered as: M1 closes most of the gap (C0 evidence), M0 does nothing (D2j-0b evidence).
- **D2r-1 (Meier):** the real axial march — stream from the radial loop, per-bin `h`,
  `body_radius`, the shield sink, the mouth restriction — built only if M1 passes, scored on
  Ø(z), floor flatness (D2m v(r)/v_c 0.83 @ 29, 0.62 @ 39 mm) and cut radius vs skirt.

Agree with: cap and `f` declared fudges and D2s (effective area via `cos θ`) as the real fix;
shield included rather than deferred (for Meier); P5 corrected; "mesh gap is a permission slip".

# Reviewer: on the planner's self-diagnosis and proposed process fixes — 2026-09-23

Diagnosis accurate (intent vs keys; dependencies under-verified; plausibility as verification;
confirmation after deciding; completeness cheap, verification expensive). Proposed fixes
(reachability ledger, legs as literal keys, "what would make this inert" with a witness per item,
assertion discipline, ledger as the /plan→/implement interface) would have caught the three
key-reachability failures. **They do not cover the second error class** — physical constants,
literature numbers and on-disk measurements (textbook k = 2.5, the §4.2.2 mantle as a chamber-loss
prior, 1436 K "rehabilitated", "no 10 mm physical length", the cap at the wrong conductivity).
**Additions:** (1) every constant/measurement/literature number carries a source location opened
this session, and each hypothesis lists the on-disk runs for AND against it before the prediction;
(2) **smoke before campaign** — a 2 mm smoke of every leg with the mechanism's witness counter
non-zero on tests and zero on controls, before any 1 mm or Meier run; (3) **frozen baselines are
re-validated** by re-running the cheapest leg and `cmp` whenever the binary changed and the
identity set does not cover the baseline's keys; (4) Context carries only what executes; argument
and history go to the log/archive; (5) guardrails that restrict verification ("do not re-run",
"do not touch X") must cite a verified reason or default to "verify if < 5 min"; (6) allow /verify
on a `ready` packet as a ten-minute pre-implementation check of the ledger and key table.
Planner's pushback on mesh-vs-shape is legitimate and already testable: D2m's v(r)/v_c = 0.62 at
39 mm is the number the drain fix must move if it is shape-relevant.

## Reviewer: D2s packet review — the residual mesh gap is the PATCH RIM, not the front — 2026-09-23

Zero runs. Reconstructed k_top(i,j,t) from the frozen `_removal_events.csv` of D2r-1 (B/S/T),
D2j-0b (C0/C1/C2) and D2l (A_f10/f05/f02/f01); scored the same disk-mean rate metric by radius.
Scripts: scratchpad `d2s_wcheck.py`, `d2s_pillars.py`, `d2s_radial.py`, `d2s_radial2.py`.

### 1. Where the D2r-1 "above-plane faces" actually are
- Front at 250 s: nozzle plane 62.4 mm, median front 40–42 mm; interior lag 5–95 % = −2…+6 mm.
  Nothing in the interior is above the plane.
- Above-plane in-patch columns: **1 at 2 mm** (col 14,3 r=29.8 mm, h=150 mm, 0 events) and
  **13 at 1 mm** (all r = 29.7–29.9 mm; 11 never spalled, 2 with 17–21 events). In B/S at 1 mm the
  ring r = 27.5–30 mm has **110 columns lagging 30–110 mm** (the D2i ring stall).
- `side_cols_above` 103 → 2441 = (1 → 13 column centres inside the last 0.3 mm of the r=30 mm
  mask) × (faces per mm of cliff). Mask-edge quantisation + cliff height ∝ t. Not roughening.

### 2. The mesh gap by radius (1 mm / 2 mm − 1, blocks 50–100 / 100–150 / 150–200 [/200–250])
| leg | r < 30 (scored) | r < 25 | 25–30 mm annulus |
|---|---|---|---|
| D2j-0b C1 (sides off) | −13.5/−28.7/−45.5 | −7.5/−27.5/−47.3 | −62/−100/stalled at both meshes |
| D2j-0b C0 (no plane) | −10.4/−15.3/−20.1 | −6.0/−12.1/−17.5 | −38/−42/−47 |
| D2l A_f10 | +3.4/+1.5/−3.2 | +4.0/+1.2/−3.8 | +1.9/+2.2/−1.7 |
| D2l A_f05 | +3.7/+1.4/−2.1 | +5.1/+0.9/−4.0 | +0.6/+2.2/+2.1 |
| D2l A_f02 = D2r-1 B | −6.4/−8.0/−14.0/−16.6 | **−0.2/+3.0/−1.8/−0.3** | −21/−33/−44/−56 |
| D2l A_f01 | −10.6/−20.2/−27.2 | −1.4/−3.7/−6.8 | −39/−73/−98 |
| D2r-1 S (tops) | −6.4/−7.9/−13.9/−16.4 | −0.2/+3.0/−1.9/−0.2 | −21/−33/−44/−55 |
| D2r-1 T (tops+sides) | −2.3/+0.7/−2.0/−0.8 | −0.0/+3.3/−1.5/+0.5 | −7.8/−5.2/−3.7/−4.0 |

Reading: D2j-0b/D2k were right — with sides off the cascade is in the interior. With f ≥ 0.2 the
**interior has converged since D2l**; D2l's f_min = 0.5 and the entire D2r line (0/0b/0c/1) were set by
the **25–30 mm annulus**, i.e. the abrupt edge of the heated patch against rock that is never removed
(a sharp corner in h — D2o-0b's own lesson). D2r-1's key fixes the rim cliff and changes the interior
by nothing.

### 3. Criterion (e) misread
T_1mm r<30 rate is flat (1.563/1.568/1.586/1.585) and equals the 2 mm level (~1.60). The
"+4.5/+9.6/+13.9/+19.1 % monotone climb" is **B_1mm decaying** (1.496 → 1.331) as its rim stalls. T
does not drill faster than the converged level; the ceiling metric compares against a sinking baseline.

### 4. D2s's formula against D2r-1's own surfaces
A staircase face exists only where the drop is ≥ 1 cell, so a face-owning column has local slope
≥ 1 and w ≥ 0.41 (1D) by construction. Face-weighted mean w on face-owning columns: 0.56–0.68
(central differences, packet formula) / 0.61–0.99 (per-edge). On the rim cliff (drop 65–147 cells)
w → 1: a vertical wall's true area IS its staircase area. The packet's w ≈ 0.11–0.18 came from a
patch-wide mean slope dominated by flat columns that own no faces. Consequences: no ×5.6 cut above
the plane (×~0.7 at most, per-edge ×~1); below the plane w ≈ 0.6 is ×3 UP on f = 0.2, so P4 is
inverted; P1/P3 refuted before launch; (a) passes trivially. The √3 bound does not transfer to the
lateral area (true lateral excess is second order in slope), so "true area grows ≥ ×3.4" does not
follow. Also: central differences vanish on a symmetric peak/trough while the peak owns the faces;
a per-edge weight from the drop across each face is the consistent form if the idea is ever revived.

### 5. Recommendation
Do not run D2s. Next packet, ~zero cost: re-score the D2r line by radius (table above), decide what
the rim is for — (i) face test → score r ≤ 25 mm, the side-face closure at f = 0.2 is already
converged, above-plane heating unnecessary; (ii) wall prototype → build a wall test on purpose:
smooth radial taper of h over several cells (kills the mask-edge quantisation and the sharp corner),
score wall lag profile / Ø(z) never disk-mean rate, heat the above-plane wall with the annulus
physics (D2r-1's key is the crude version). Redesign (e) against the 2 mm / interior level.

## Reviewer: D2t (smooth radial taper + shape metric) — 2026-09-23

Direction agreed: remove the cliff, score z(r)/D(z), no disk-mean rate. Code claims checked: `h_f` is a
5-arg executor over (x,y,r,s,t) (:5940, :6304), positivity abort at :1787, top/side masks at :1579/:1626.

Defects:
1. **(b), (c), P1, P2 misidentify the witness.** A smooth taper CREATES a lagging annulus by
   construction: the core at h=700 makes ~1.63 m/h vs feed 1.55, so any column with h(r) below
   ~0.95·peak falls behind the plane; once above it the TA form gives it 142·taper(r) ⇒ ~0.25 m/h
   ⇒ the whole band 19–34 mm becomes an above-plane wall: ~620 columns at 1 mm, ~160 at 2 mm
   (vs bar 15 / refute 40). PA vs P then acts 5× on a legitimate wall, not on a cliff ⇒ not quiet.
   Replace (b) with smoothness (max adjacent-bin step of z(r) inside r_taper; zero never-spalled
   columns where h ≥ 0.5·peak). Replace (c) with PA_1mm vs PA_2mm convergence; PA−P is the first
   measurement of what the annulus lever does to a wall slope — a result, not a null.
2. **Item 2 has no implementation path.** `flame_h` is never written to plotfile/thermo/csv. Proof of
   the COMPILED profile: a short 2 mm run (seconds, before any spall) and top-cell T per column from
   the plotfile — early ΔT_s ∝ h(r)(T_g−T_0). Numpy re-evaluation is a filter only (the a/max bug lived
   in the parser).
3. **(a) bar 4 mm over every bin to r_taper** is a core bar applied to a 50–100 mm-lag knife-edge band.
   Gate the core (r < r_flat) at 4 mm; gate the band via D(z) on the depth ladder at one 2 mm bin
   (4 mm in diameter), i.e. make (e) the band's gate instead of "reported".
4. (d) reference −0.0/+3.3/−1.5 is the r<25 mask; recompute the frozen reference at r < r_flat = 18 mm.
5. Minor: the floor annulus 34–40 mm now carries surface losses (in_patch) while r>40 does not — a
   weak second edge in the loss term; negligible at ambient, note it or push radius to 0.05.

## Reviewer: state of the command files after the 14:20 edit — 2026-09-23

Landed: plan.md "Verification before writing" (ledger, literal keys, sources opened this session and
taken where consumed, refuting runs, inertness witnesses, context executes-only, guardrails-are-
assertions); implement.md "Step zero" (verify ledger + key table, diff harness commands, re-validate
frozen baselines outside the identity set, smoke before campaign); verify.md pre-implementation check
on a `ready` packet (ledger/keys/sources only).

Coverage: these address the D2r-0/0b/0c class (unreachable legs, phantom controls). They did NOT fire
on D2s or D2t, whose defects are of a different class:
1. Planner-derived numbers with no external source (D2s "mean |grad h| 0.46" on the wrong population;
   the √3 bound misapplied). Rule: a number the planner computes itself carries the script path IN
   THE STUDY DIR and the population/mask it was computed on; /verify pre-check re-runs the script.
2. Witnesses with no output path (D2t item 2: flame_h is written nowhere). Rule: every witness and
   pre-flight names the file/column it is read from.
3. Non-discriminating criteria (D2t (b)/(c): fail under the hypothesis too). Rule: for each
   criterion write its expected value with the mechanism ABSENT and PRESENT; overlap ⇒ not a criterion.
Add these three to plan.md §Verification and to verify.md's pre-check list; ~6 lines.

## Reviewer: D2t completed — reading and next step — 2026-09-23 evening

Implemented as revised (my five fixes all taken: thermal trace for item 2, D(z) gate for the band,
(b) as never-spalled-where-h≥0.5, (d) reference on r<18, radius 0.050; s-switch reported as (e)).
Core converged (2.00 mm = one coarse cell, uniform offset). Band: z(r) differs 34 mm (P) / 16 mm
(PA) at r=23 but D(z) passes — the wall is vertical, so this is a ONE-COARSE-CELL radial shift of the
latch radius, not a depth divergence. The physically important number: **D at depths 40–60 mm is
46 mm at 2 mm vs 42 mm at 1 mm with the lever OFF (P), and 46 vs 46 with it ON (PA).** Band rate gap
−7→−49 % (P) vs −6→−6 % (PA). ⇒ the wall radius is mesh-dependent through the step drain, and the
above-plane side heating is what pins it. That is the result to carry to Meier, not "(a) passed".
Caveats: D(z) lattice (4 mm) equals its bar — need an interpolated crossing radius before any Meier
Ø claim (4 mm = 5 % of Ø85, the whole budget); latch = 4.9× h step at s=0 defines the wall; (d) −5.0 %
at 2 mm = outer core bins draining into the new wall (r≤11 is −2…−3 %, ≈ one cell).

Next: /verify (check the four items above), then the Meier cylindricity packet at 2 mm/600 s
(15 min) with side_face f=0.2 + factor_above=1.0 ON, a prescribed per-bin annulus h above the plane
(the D2r-0c 81/142/250 band, not h=0/293 K), an interpolated Ø(z) + volume metric, and the s-switch
smoothed over a few mm or declared. 1 mm (3 h) only if the 2 mm shape lands in the Meier band. Third
mesh and AMR stay parked.

### Addendum: planner's D2t conclusion checked + the (d) diagnostic (zero runs)
- Planner's two write-up corrections CONFIRMED from analysis.txt: P D(z) is 4 identical / 6 one-bin,
  D_1mm ≤ D_2mm everywhere; P4's step clause refuted (steps 45–71 mm vs "~2 bins").
- What the planner missed: PA's D(z) is 7 identical / 3 one-bin, and at depths 40–60 mm P goes 46→42
  while PA stays 46→46. The one-directional bias it flags is the lever-OFF case; the lever removes it.
  That contrast, not the rate table alone, is the result.
- (d) diagnostic, recession at 250 s per bin, T (sharp) vs P (taper): r ≤ 11 mm: −2.0 mm at BOTH
  meshes (so not pure quantisation — a uniform ~1.8 % slowdown); r = 13/15/17/19: −3.1/−3.5/−2.5/−4.1
  (2 mm) and −2.2/−3.2/−4.5/−6.6 (1 mm). ⇒ (d)'s −5 % = ~2 % uniform (smaller hot footprint, larger
  3-D loss) + an outer-bin drain into the new wall growing toward r_flat. Consistent with the drain
  mechanism, not a taper bug; a wider taper would grow the second term.

## Reviewer: D2u (Meier cylindricity) — DO NOT RUN AS WRITTEN — 2026-09-23 late

Code claims verified: `:1773` zeroes above-plane h under jet_enthalpy, `:1781` Tg = jet_T_ent (293.15,
never mutated; the march skips s ≤ 0 at :2212), `input_feet` has no side_face key, radius 0.2 covers
the quadrant (max r 0.17). Feet: z_n = z_pad + 0.050.

**1. FATAL — the scalar `jet_h_above` is a blanket over the block top.** Every column with r ≤ 0.2 and
s ≤ 0 gets it, and the whole free surface goes above the plane once the pads have receded 50 mm
(~120–150 s). D2i LI0_2mm at 572 s: 3082 of 3600 columns above the plane, 2295 of them untouched free
surface = 367 cm² (×4). At 142 W/m²K, 1600 K the pinned flux is ~80 kW/m² ⇒ 0.25 m/h ⇒ ~30 mm off the
entire block top in the remaining ~450 s ≈ +1.1 L against Meier's 3.42 L total. (D2j-0b C2 was inert
only because 100×(1000−822) < losses; 142×778 is 4× the losses, and D2t's band drilled 0.2–0.45 m/h at
exactly this h.) D2o-0's "1400 K blanket" again. The above-plane state must be radially confined to the
annulus (expression in r, smooth per D2t), free surface → h ≈ 0.

**2. P2 is refuted by data on disk.** Interpolated Ø(z) [mm] from `d2i_gate/output/LI0_2mm` (572 s):
| depth | 10 | 25 | 50 | 75 | 100 | 125 | 150 | 175 | 200 |
| model | 144 | 129 | 114 | 105 | 99 | 96 | 93 | 90 | 87 |
| Meier csv | 96 | 92 | 88 | 87 | 87 | 87 | 86 | 85 | 85 |
`d2m_feet/R40` (250 s) gives the same 144/129/114/99 at 10/25/50/100. Mean Ø 50–200 = 98 mm, never below
87. The model is too WIDE at the top by 48 mm (collar 144→99 over 100 mm vs Meier's 96→87), near-right
at 150–200 mm. D2m's "min Ø ≈ 80 mm" is the narrowest section near the front, not Ø(z) at 50–200 mm.
⇒ the lever (widening) points AGAINST the dominant error at 10–100 mm depth.

**3. The collar is formed BELOW the plane, early, and then freezes.** LI0 Ø at 10/25/50 mm is 144/129/114
already at 200–340 s and unchanged to 572 s; mouth ring r 50–76 mm recedes 32.5 mm by 340 s and stops;
r > 76 mm recedes 0.64 mm. Cause = the wall-jet `h_expr` spreading over the free surface at r 45–76 mm
while s > 0. Physically that region is under the 80 mm skirt's slots / in the exhaust annulus, not
under a wall jet (meier-burner-is-a-body-in-the-hole). This is D2o-0's floor/wall split, which failed
on a sharp corner (jam) and a cascade (D2o-0b) — both now handled (D2t smooth taper, D2k/D2r-1 sides).

**4. Smaller:** M1 is not a clean control — with h=0/293 K above the plane the side faces LOSE heat
(SurfaceCellFlux Newton with h=0 → q_in = G(Tf−Tc) < 0); set side_face_factor_above=0.0 on M1. Domain:
input_feet is 0.3 m; the pit (deepest column) reaches 248 mm at 340 s and ~300 at 440 s ⇒ 600 s is
impossible; use the 0.4 m input (input_feet_d2c / D2i keys) and stop ≈ 500 s. dt: input_feet is 4 ms;
16 ms is proven at 2 mm (dt ladder) ⇒ the 15-min estimate is ~4× low at 4 ms. Reference: two
digitisations disagree by 8–10 mm at mid-depth (csv 85–87; 09-22 rim read 95 ± 5, lit-lip bias) ⇒ score
against a band [85, 95] ± 5 and the csv for shape only. T_above: jet_T_exhaust ≈ 1660 K uncooled; the
jacket (186 W/K) is not modelled — declare the drop.

**Recommendation:** re-plan as the body-in-the-hole h map: (i) confine the impingement/wall-jet h to
r ≤ foot_r_outer with a smooth taper (≥ 8 coarse cells) to the annulus value by r ≈ 48–50 mm, in BOTH s
branches; (ii) above the plane evaluate an expression (not a scalar) with T_gas = declared exhaust, floor
1e-3 beyond r ≈ 60 mm; (iii) legs M0 / M1 (radial confinement only, above-plane 0, factor_above 0) /
M2 (M1 + annulus wall heating, side 0.2/1.0) ⇒ attribution funnel vs wall; (iv) interpolated Ø(z) +
volume, 0.4 m domain, 16 ms, ~500 s; (v) pre-register M0 = the table above.

## Reviewer: D2v (body-in-the-hole h map) — agree with the plan, five fixes before launch — 2026-09-23

Verified: the packet's h_expr radial table (686 at r=40, 362 at 76, 164 at 168 W/m²K; s-clamp gives 603
at r=46 for s ≤ 0); the blanket count; LI0 = input_drilling + D2c/D2h keys in 0.4 m at 16 ms.
1. **Above-plane map is wrong inside r_ann.** "Same expression above the plane" with s clamped to 15 mm
   gives the above-plane wall at r 42–50 mm h ≈ 600–550 at T_above ≈ 1690 K ⇒ ~1 m/h wall recession
   ABOVE the plane ⇒ the hole is driven out to r_ann (Ø ≥ 104) by construction. The packet's "w1 has
   already handed over at the wall" is false (w1 ≈ 0.6–0.3 at 42–47 with r_imp 36 / r_ann 52). Key-only
   fix: branch on s inside the one expression, `if(s>0, h_imp*w1 + h_ann*(1-w1), h_ann)*w2 + h_out*(1-w2)`;
   the source change stays "evaluate h_f for all s when jet_above_h = 1".
2. **r_imp = 36 mm cuts into the pad annulus (28–40).** Outer pad rock at h ≈ 600 vs 686 ⇒ the
   0.9-quantile carriers slow ⇒ feed slows (D2o-0's jam mechanism, mild). Put r_imp = foot_r_outer = 40,
   r_ann = 56. Pre-register the numerical floor: a 16 mm taper centred at 48 mm cannot cut narrower than
   roughly Ø 2·(r_imp + W/2) ≈ 96–100 mm; taper width is a numerical parameter to be tested (8 mm at
   1 mm mesh), never tuned.
3. **M0 ≠ LI0.** input_feet_d2c has pinned_idle_cycles 2.0 (LI0: 1e9) and foot_body_clearance 1 (LI0: 0),
   so P1 (±5 mm vs the LI0 table) can fail for configuration reasons and be misread as a finding. Build
   M0 from LI0's exact keys (import d2h/d2i harness by path, stop 500) and make P1 byte identity of the
   event log to LI0 up to 500 s — or drop the leg and score LI0 frozen.
4. **Item 3 trace is under-specified for this branch.** `_jet_profile.csv` writes T_gas per bin (1207 K
   at t = 0, rising) but not h; the early ΔT must be normalised by the march's T_gas(r). And with feet
   every column starts at s = +50 mm, so a few-second run never exercises the above-plane branch —
   add D2t's TR_above analogue: nozzle_descent=prescribed with the plane below the surface, same
   h_expr, jet_closure=enthalpy, collision guard off.
5. Minor: (d)'s 2 mm bar beyond 76 mm is fine (M0 0.64 mm); state that above-plane TOPS inside the
   mouth are heated too (TA form, as D2t); T_above declared with the jacket drop.

### D2v second revision (19:30) — all five fixes taken; three residuals
1. Item 2 still carries the stale paragraph "h_imp clamps s … the radial confinement is what makes
   this safe — at the wall w1 has already handed over" — contradicts item 1's flat wall branch; delete
   it or the implementer may build the blended above-plane map. Item 1's blended-h table is for the old
   36–52 linear ramp (557 at r=40); label it as the rejected form.
2. P6 overclaims. The "Ø ≥ 96–100 by construction" floor holds only at shallow depth. A wall column at
   r = 46 (h ≈ 455 below the plane, v ≈ 1.16 m/h vs feed 1.5) stays below the plane ~530 s and cuts
   ~160 mm; at r = 50 (h ≈ 268, v ≈ 0.65) it is overtaken after ~210 s having cut ~38 mm. So expect
   Ø ≈ 100 at 50 mm, 92–96 at 100–160 mm, and ~88–92 deeper on M1 — and M0 reads 80–87 at 200 mm.
   "Refuted if any leg cuts below 92 mm at depth" would fire on M0 and possibly on M1 for depths still
   forming at 500 s. Restrict P6 to M1/M2 at 10–50 mm; make the deep range a report.
3. The prescribed-descent trace leg must strip `jet_free_surface=1` (aborts without feet, :6214) and
   `foot_body_clearance` (:6286), and the other foot_* keys will be strict-checked as unused — list the
   exact key set for that leg. Also note the new map extracts less power on the block top, so M1/M2's
   own exhaust T will sit ABOVE LI0's 1690 K; the declared T_above is then on the low side — direction
   stated, not adjusted.
