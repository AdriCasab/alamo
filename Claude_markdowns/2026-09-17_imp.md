# 2026-09-17: D2c implementation (closures and pre-registration)

Session: `/implement` of ACTIVE_STEP D2c. No scored run, nothing committed.

## Done

- **Code** (`MMWSpalling.H`, `Removal.H`; all default-off):
  - momentum decay diameter;
  - free-surface dilution, with T_rec taken from the mouth;
  - exponential bin update;
  - signed rock-to-gas flux;
  - burner-body clearance (regime 4, flipped by index at the next step's
    removal pass).
- **Far h law:** `walljet.h_expr(far="power", n)`. `feetmodel.Cfg` carries the
  same law, with its default output byte-identical.
- **Tests:**
  - `unit/jet_d2c` passes (26 checks).
  - All targeted witnesses pass, the dev2d and S1 smoke runs exit 0, and
    2639/2639 hashes are identical.
- **`studies/d2c_steady`:**
  - `nozzle.py` reproduces the packet table. J includes the pressure thrust
    (19.15 N; the packet's 19.3 N is ṁ·u_e).
  - `handmodel.py`: steady and transient hand model.
  - `PREDICTIONS.md`, hashed at 13:54 (sha256 a28f1987…).
  - `score.py`, with the dry run on D2b.
  - `run.py`: the D2d harness, probes and `--write-input`.
- **New input:** `sp_meier_pilot/input_feet_d2c` (parse smoke equals the
  probe).
- **Errata and notes:** Meier reference items 10–11, a D2c note in the D2b
  RESULTS §0, and a D2c section in the Meier README.

## Main pre-registered findings (see PREDICTIONS.md / RESULTS.md §3–4)

- **Steady state:** ring ROP 1.93 m/h with s_c 180 mm. The ring barely
  depends on n.
- **No steady window at 0.40 m.** The hand window opens at 750 s and needs a
  ≥ 0.65 m domain. Run 1 should hit the bottom at about 500 s.
- **The mouth stays too wide** (134/118 mm at 25/50 mm), against 174 mm at
  25 mm with the D2b closures.
- **Run 7** (1 mm, full domain) needs about 24 h for a window. The proposed
  cap is 750 s at 0.40 m.
- **2 mm runs** take about 1 h each. Night 1 runs R1/R2/R6, then R4/R5/Oa,
  then Ob.
- **The hand model vs D2b:** ROP +13 %, s_c +6 %.

## Open for D2d / user

- The domain height, or scoring without a steady window (P1).
- The run 7 cap.
- The literature check of the free-surface aspect/exponent has not been
  done.
