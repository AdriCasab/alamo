# agent.md

Codex should use `AGENTS.md` as the restart instruction file for this project.
This lowercase file exists only as a pointer because the requested name was
`agent.md`, while the conventional Codex project-instruction filename is
`AGENTS.md`.
